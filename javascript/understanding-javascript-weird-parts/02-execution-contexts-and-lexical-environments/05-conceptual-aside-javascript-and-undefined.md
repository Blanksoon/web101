# 05 Conceptual aside javascript and undefined

#### Undefined

Create by execution create phase or hoisting


```javascript
console.log(a) // undefinded
var a = 'nqmt'

// checking data
if (a === undefined) {

}

// don't set undefined value
var a = undefined
```